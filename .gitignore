# Private working notes, raw voice-to-text conversations + personal context.
# NEVER commit. Lives locally only.
rambles-personal-bio-conversations.md

# Cross-session handoff notes. Ephemeral, can rot, regenerated as needed.
SESSION_NOTES.md

# macOS
.DS_Store

# Local Claude Code settings (per-machine preferences, not project rules)
.claude/settings.local.json
.mobile-screenshots/

# Scratch / debug artifacts (screenshots, server logs, agent output dumps).
# Created by tooling during development, never meant to ship.
.tmp/

# Resume research notes (private working doc; never meant to ship).
resume/RESEARCH_HANDOFF.md
